#include "Shared.xcconfig"

DEVELOPMENT_TEAM = $(REDIRECT_DEVELOPMENT_TEAM)
PRODUCT_BUNDLE_IDENTIFIER = $(REDIRECT_APP_BUNDLE_IDENTIFIER)
INFOPLIST_KEY_RedirectExtensionBundleIdentifier = $(REDIRECT_EXTENSION_BUNDLE_IDENTIFIER)
